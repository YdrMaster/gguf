[alias]
xtask = "run --package gguf-utils --release --"
debug = "run --package gguf-utils --"
show = "xtask show"
split = "xtask split"
merge = "xtask merge"
cast = "xtask cast"
convert = "xtask convert"
to-llama = "xtask to-llama"
set-meta = "xtask set-meta"
